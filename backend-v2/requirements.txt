# ScanG backend — install: pip install -r requirements.txt
# Trimmed from Emergent platform template (removed emergentintegrations, litellm,
# and other unused packages not required by this app).

# API server
fastapi==0.110.1
uvicorn==0.25.0
python-dotenv==1.2.2
python-multipart==0.0.32

# Database
motor==3.3.1
pymongo==4.6.3

# Data / validation
pydantic==2.13.4
numpy==2.4.6
pandas==3.0.3
cachetools==7.1.4
curl_cffi==0.15.0

# Auth
firebase-admin==6.5.0
slowapi==0.1.9

# Tests
pytest==9.1.1
requests==2.34.2
httpx==0.28.1

# Portfolio document extraction (PDF/image, local OCR)
pdfplumber==0.11.4
pymupdf==1.24.14
Pillow==11.1.0
pytesseract==0.3.13

# Payments (Razorpay Premium subscriptions)
# razorpay 2.x dropped pkg_resources (removed in setuptools 82). Keep 1.4.2 + setuptools<82
# only if you cannot upgrade; 2.0.0+ is the correct fix on Render/Python 3.12.
razorpay==2.0.0
 